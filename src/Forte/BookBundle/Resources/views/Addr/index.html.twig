{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Addr list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Bairro</th>
                <th>Rua</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('addr_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.bairro }}</td>
                <td>{{ entity.rua }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('addr_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('addr_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('addr_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
