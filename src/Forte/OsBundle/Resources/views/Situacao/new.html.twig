{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Situacao creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('situacao') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
