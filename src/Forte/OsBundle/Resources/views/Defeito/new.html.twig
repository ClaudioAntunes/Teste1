{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Defeito creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_os_defeito') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
