{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reparo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Servico</th>
                <th>Duracao</th>
                <th>Valor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reparo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.servico }}</td>
                <td>{{ entity.duracao }}</td>
                <td>{{ entity.valor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reparo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reparo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reparo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
